/**
 * Values that are chosen rather than computed.
 *
 * Kept apart from the code that reads them so that changing how the scene
 * looks does not mean editing the code that draws it.
 *
 * @packageDocumentation
 */

/** Scene tuning: exposure, density, grain, the per-phase multipliers. */
export { tuning } from "./tuning";
