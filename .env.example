# ECHO runs entirely in the browser and talks to no service, so there is
# nothing to configure. This file exists to say that on purpose rather than
# by omission: if you are looking for the API keys, there are none.
#
# The only build-time variable is set by CI, not by you:
#
#   GITHUB_PAGES=1   sets the base path to /ECHO/ for GitHub Pages.
